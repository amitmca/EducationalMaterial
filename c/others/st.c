#include<stdio.h>
#include "conio.h"
#include<string.h>

main()
	{
	char str[20];
	clrscr();	
	printf("\nEnter the string:");
	gets(str);
//	scanf("%s",str);
	puts(str);
//	printf("\nThe String is:%s",str);
	getch();
	}
